/**
 * 2018/2/5 write by luyaj
 * @param { 浮点数 } base 
 * @param { 整数 } exponent 
 * 面试题16：给定一个double类型的浮点数base和int类型的整数exponent。
 * 求base的exponent次方。
 */
function Power(base, exponent)
{
    return Math.pow(base, exponent);
}